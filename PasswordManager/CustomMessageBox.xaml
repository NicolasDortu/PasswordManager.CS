<Window x:Class="PasswordManagerWPF.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Height="100" Width="200"
        WindowStartupLocation="CenterScreen"
        Icon="Resources/shield.ico"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">
    <Grid>
        <Border Background="#FF9681D8" BorderBrush="#FF521FE4" BorderThickness="2" CornerRadius="10">
            <StackPanel Margin="10">
                <TextBlock x:Name="MessageTextBlock" Text="Message Text" Foreground="White" Margin="0,0,0,25" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK" Width="80" Margin="5" Click="OkButton_Click" Background="#FF521FE4" Foreground="White" BorderBrush="#FF707070"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
